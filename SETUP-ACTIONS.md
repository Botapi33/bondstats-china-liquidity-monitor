# Setup Actions

If GitHub does not show **Run workflow**, verify this exact path exists in the repository:

`.github/workflows/china-liquidity-monitor.yml`

On macOS Finder hidden folders are shown with:

`⌘ + Shift + .`

Then set GitHub Pages to **GitHub Actions** and run the workflow manually once.
